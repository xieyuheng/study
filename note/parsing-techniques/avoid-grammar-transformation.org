#+title: avoid-grammar-transformation

* [note]

  - we must try to avoid grammar transformations
    for error report
    and easy generation of structures

  - if we can not avoid grammar transformations
    special layer of abstraction over them
    must be designed
