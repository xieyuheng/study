import org.scalatest._
import xieyuheng.cicada._

class CicadaSpec extends FlatSpec with Matchers {
  "placeholder" should "pass" in {
    assert(true)
  }

  it should "pass again" in {
    assert(true)
  }
}
